[package]
name = "akatable"
version = "0.2.0"
entrypoint = "lib.typ"
authors = ["Simon Buré"]
license = "MIT"
description = "Clean academic tables with publisher format presets (APA, IEEE, Nature, etc.)."
repository = "https://github.com/SimonBure/akatable"
keywords = ["tables", "academic", "booktabs", "formatting"]
categories = ["paper", "report", "thesis"]
compiler = "0.14.0"